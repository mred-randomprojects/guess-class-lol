# Development Guidelines

## Build Verification

After every code change, run `npm run build` to verify the project compiles successfully before reporting back. This catches TypeScript errors and build issues early.
